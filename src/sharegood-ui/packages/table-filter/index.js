import SgTableFilter from './table-filter';

/* istanbul ignore next */
SgTableFilter.install = function(Vue) {
    Vue.component(SgTableFilter.name, SgTableFilter);
};

export default SgTableFilter;
